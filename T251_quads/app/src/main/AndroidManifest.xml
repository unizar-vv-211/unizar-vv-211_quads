<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Declaración de paquetes consultables (necesario para Android 11+) -->
    <queries>
        <package android:name="com.whatsapp" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        
        <!-- Pantalla principal - punto de entrada -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.QuadEdit"
            android:exported="false" />

        <activity
            android:name=".ui.ReservaListActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ReservaEdit"
            android:exported="false" />
            
        <activity
            android:name=".ui.QuadListActivity"
            android:exported="false" />
            
        <!-- Activity para ejecutar pruebas unitarias -->
        <activity
            android:name=".ui.TestActivity"
            android:exported="false"
            android:label="Pruebas" />
    </application>

</manifest>